{
  "ignoreCommand": "git fetch --deepen=1 2>/dev/null; ! git diff --name-only HEAD^ HEAD | grep -qE '^(apps/fleet|packages)/'",
  "crons": [
    {
      "path": "/api/notify-reminder",
      "schedule": "0 13 * * *"
    }
  ]
}
