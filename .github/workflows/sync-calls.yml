# ═══════════════════════════════════════════════
# TowBook → Supabase Scheduled Calls Sync
#
# Runs every 5 minutes (GitHub's floor; scheduled runs are best-effort and can
# drift — the live dispatch board shows a staleness warning when they do).
# Scrapes the TowBook dispatch page with Playwright and upserts jobs in Supabase.
#
# Required repository secrets (Settings → Secrets → Actions):
#   TOWBOOK_USER         — TowBook login username
#   TOWBOOK_PASS         — TowBook login password
#   SUPABASE_URL         — from Supabase Dashboard → Settings → API
#   SUPABASE_SERVICE_KEY — service role key (bypasses RLS) — NOT the anon key
#   GEOCODIO_KEY         — from geocod.io dashboard (optional — improves address quality)
# ═══════════════════════════════════════════════

name: Sync TowBook Calls

on:
  schedule:
    - cron: '*/5 * * * *'    # every 5 minutes (best-effort)
  workflow_dispatch:           # allow manual runs from the Actions tab

# Geocoding can push a run past 5 minutes — never let two scrapes overlap.
concurrency:
  group: sync-calls
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 10       # kill if TowBook login hangs

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: apps/transport/requirements.txt

      - name: Install Python dependencies
        run: pip install -r apps/transport/requirements.txt

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-${{ hashFiles('apps/transport/requirements.txt') }}
          restore-keys: playwright-chromium-${{ runner.os }}-

      - name: Install Playwright browsers and system deps
        run: python -m playwright install chromium --with-deps

      - name: Run sync
        env:
          TOWBOOK_USER:        ${{ secrets.TOWBOOK_USER }}
          TOWBOOK_PASS:        ${{ secrets.TOWBOOK_PASS }}
          SUPABASE_URL:        ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          GEOCODIO_KEY:        ${{ secrets.GEOCODIO_KEY }}
        run: python apps/transport/scripts/sync_calls.py
